import structureMinistryData from "/data/Structure.json";

import {
   Table,
   Button,
   Modal,
   Form,
   Col,
   Tag,
   Row,
   Input,
   Select,
   Menu,
   Dropdown,
} from "antd";

const ModalEditRole = () => {
   return (
      <div>
         <p>Modal</p>
      </div>
   );
};

export default ModalEditRole;
